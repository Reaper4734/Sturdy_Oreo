server:
  port: 8080

spring:
  cache:
    type: simple
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/oreo_db}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 15
      minimum-idle: 5
      connection-timeout: 20000

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

oreo:
  jwt:
    secret: ${JWT_SECRET:9a8b7c6d5e4f3a2b1c0d9e8f7a6b5c4d3e2f1a0b9c8d7e6f5a4b3c2d1e0f9a8b}
    expiration-ms: 86400000 # 24 hours
  auth:
    google-client-id: ${GOOGLE_CLIENT_ID:test-client-id.apps.googleusercontent.com}
  llm:
    gemini-api-key: ${GEMINI_API_KEY:dummy-gemini-key}
    openai-api-key: ${OPENAI_API_KEY:dummy-openai-key}


logging:
  level:
    root: INFO
    com.oreo: DEBUG
    org.springframework.security: INFO
    org.hibernate.SQL: DEBUG

resilience4j:
  circuitbreaker:
    instances:
      geminiCircuitBreaker:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        waitDurationInOpenState: 10s
        failureRateThreshold: 50
        eventConsumerBufferSize: 10
